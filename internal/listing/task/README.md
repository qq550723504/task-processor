# Listing Task

Owns listing task lifecycle, task models, task-scoped resource resolution, and task-oriented orchestration that is not platform-specific.

This package exposes provider-neutral contracts only. Persistence adapters may implement these contracts, but callers must not depend on legacy ListingKit task DTOs.
