# Code Health Decisions

| Finding | Owner | Evidence | Decision | Verification |
| --- | --- | --- | --- | --- |
| `internal/listingkit/core/string_helpers.go` (`joinStrings`, `formatInt`) | `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing` | All four post-edit reports (`.local/code-health/20260813-012330715/deadcode-root-windows-prod.json`, `.local/code-health/20260813-012330715/deadcode-root-windows-test.json`, `.local/code-health/20260813-012330715/deadcode-root-linux-prod.json`, `.local/code-health/20260813-012330715/deadcode-root-linux-test.json`) confirm the removed declarations are absent; pre-delete `rg` found declarations only in this package. | removed | `go test ./internal/listingkit/... ./tests/... -count=1`; `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing`; `pwsh -File scripts/code-health-audit.ps1 -Mode Go` (manifest `.local/code-health/20260813-012330715/manifest.json`) |
| `internal/app/httpapi/listingkit_temporal_worker.go` (`RunListingKitTemporalWorker`) | `internal/listingkit/httpapi` Temporal runtime | The same four post-edit reports (`.local/code-health/20260813-012330715/deadcode-root-windows-prod.json`, `.local/code-health/20260813-012330715/deadcode-root-windows-test.json`, `.local/code-health/20260813-012330715/deadcode-root-linux-prod.json`, `.local/code-health/20260813-012330715/deadcode-root-linux-test.json`) confirm `RunListingKitTemporalWorker` is absent; exact references contain only boundary tests/documentation, while `internal/listingkit/httpapi.BuildTemporalRuntime` is the active runtime owner. | removed | `go test ./internal/app/httpapi ./internal/listingkit/httpapi ./tests/... -count=1`; `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing`; `pwsh -File scripts/code-health-audit.ps1 -Mode Go` (manifest `.local/code-health/20260813-012330715/manifest.json`) |
| `internal/app/consumer/task_submitter.go` (`TaskSubmitter`) | `go test ./internal/app/consumer` | The four post-edit reports above still mark the concrete constructor and methods unreachable from maintained command roots, but `internal/app/consumer/task_submitter_test.go` directly exercises the concrete type and behavior; the detector does not distinguish this direct unit-test contract from production reachability. | detector-limitation | `go test ./internal/app/consumer -count=1`; retain for a separately reviewed test/compatibility decision |
| `internal/app/consumer/platform_processor_registry.go` (`RegisterAllProcessors`) | `internal/app/runtime/listing` | The four post-edit reports above mark only the convenience method unreachable; constructor, `RegisterPlatforms`, and `SharedResourceNeeds` are reached by `internal/app/runtime/listing/runtime.go`, so the file is active and cannot be deleted as a leaf. | retained-deferred | `go test ./internal/app/consumer ./internal/app/runtime/listing -count=1`; `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing` |
| `internal/listingadmin/raw_json_data.go` (`listingRawJSONDataFromRawJSONData`) | `internal/listingadmin` | The normalized four-report intersection from `.local/code-health/20260813-014254252/deadcode-intersection.json` marked the private converter in all Windows/Linux production/test modes. Exact `rg` found only its declaration; the repository uses `listingRawJSONData.toRawJSONData` for the maintained read path and has no reverse conversion call site or registration. | removed | `go test ./internal/listingadmin -count=1`; `rg -n "listingRawJSONDataFromRawJSONData" internal cmd scripts tests deployments config -g '*'` (no matches) |
| `internal/listingkit/generation_action_keys.go` (`allowedAssetGenerationActionKeys`) | `internal/listingkit` | The normalized four-report intersection from `.local/code-health/20260813-014254252/deadcode-intersection.json` marked the private wrapper in all modes. Exact `rg` found only its declaration; action validation calls the canonical `generation.IsAllowedActionKey` path and no caller consumes an allowlist slice. | removed | `go test ./internal/listingkit -count=1`; `rg -n "allowedAssetGenerationActionKeys" internal cmd scripts tests deployments config -g '*'` (no matches) |
| `internal/listingkit/generation_conditional_state.go` (`buildGenerationConditionalETag`) | `internal/listingkit` | The normalized four-report intersection from `.local/code-health/20260813-014254252/deadcode-intersection.json` marked the private adapter in all modes. Exact `rg` found only its declaration; conditional response builders use `buildGenerationConditionalState` and no code calls this standalone ETag adapter. | removed | `go test ./internal/listingkit -count=1`; `rg -n "buildGenerationConditionalETag" internal cmd scripts tests deployments config -g '*'` (no matches) |
| `internal/listingkit/export_platform_visual_base.go` (unused export-base constructor) | `internal/listingkit` | The same four-report intersection plus exact `rg` showed no caller; maintained export payload inputs call `buildPlatformVisualPresentationBaseForPlatform` directly. | removed | `go test ./internal/listingkit -count=1`; exact `rg` reference check |
| `internal/listingkit/preview_platform_visual_payload.go` (unused preview-base identity adapter) | `internal/listingkit` | The same four-report intersection plus exact `rg` showed no caller; maintained preview payload inputs construct the canonical presentation base directly. | removed | `go test ./internal/listingkit -count=1`; exact `rg` reference check |
| `internal/listingkit/platform_helpers.go` (`buildPlatformAttributes`, `parseFloatDefault`) | `internal/listingkit` | Both private helpers appeared in all four reports and exact `rg` found declarations only; no route, registration, or serialization boundary references them. | removed | `go test ./internal/listingkit -count=1`; exact `rg` reference check |
| `internal/listingkit/service_upload_logic.go` (`uploadImagesWithLegacyStore`) | `internal/listingkit` | The private batch wrapper appeared in all four reports and exact `rg` found only its declaration; active uploads use the per-file legacy path or keyed repository path. | removed | `go test ./internal/listingkit -count=1`; exact `rg` reference check |
| `internal/listingkit/text_match.go` (`containsText`) | `internal/listingkit` | The private helper appeared in all four reports and exact `rg` found only its declaration; no active ListingKit workflow or test references it. | removed | `go test ./internal/listingkit -count=1`; exact `rg` reference check |
| `internal/core/config/config.go` (`dotEnvCandidates`, `dotEnvCandidatesForConfig`) | `internal/core/config` | The normalized jscpd report `.local/code-health/20260813-021101268/jscpd.txt` found two identical path-cleaning/deduplication tails in one package. Both callers require first-occurrence order and platform-native `filepath.Clean`; extraction preserves those semantics and adds a characterization test. | consolidated | `go test ./internal/core/config -count=1`; post-edit jscpd `.local/code-health/20260813-021313565/jscpd.txt` (425 clones, target pair absent) |

## Deferred and compatibility subgraph classifications

The following rows classify the Task 8 four-way intersection from
`.local/code-health/20260813-014254252/deadcode-intersection.json`. Counts are
unique declarations after normalizing the Windows/Linux and production/test
reports (each declaration appeared in all four reports). These are exact
package owners, not wildcard decisions for an entire platform tree.

| Finding | Owner | Evidence | Decision | Verification |
| --- | --- | --- | --- | --- |
| `internal/amazon/listing` (47 declarations) | `internal/platforms/amazon` conditional processor module | `docs/refactoring/current-refactoring-status.md` §2.2 and README §2.2 state that Amazon target-listing code is retained while the full workbench is deferred; `internal/platforms/amazon/module.go` registers the processor when `cfg.Amazon.Enabled`; `go list -deps` includes the package from maintained roots. | retained-configuration-specific | `go test ./internal/amazon/listing ./internal/platforms/amazon -count=1`; maintained command builds; four-way intersection evidence above |
| `internal/amazon/model` (25 declarations) | Amazon processor and API model boundary | The same Amazon maturity authority applies; model types are consumed by `internal/amazon/processor.go`, `internal/amazon/api`, and Amazon marketplace publishing packages, so absence from the default SHEIN path is not retirement evidence. | retained-configuration-specific | `go test ./internal/amazon/model ./internal/amazon -count=1`; `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing` |
| `internal/amazon/pipeline` (6 declarations) | Amazon processor pipeline and package-owned tests | `internal/amazon/pipeline/*_test.go` is a maintained test entrypoint for mapping, quota, upload, and status behavior; `internal/platforms/amazon/module.go` is the runtime registration owner. | retained-configuration-specific | `go test ./internal/amazon/pipeline ./internal/platforms/amazon -count=1`; exact intersection report |
| `internal/amazon/schema` (48 declarations) | Amazon schema manager used by the listing pipeline | `internal/amazon/listing/*` imports schema builders/managers; the Amazon target workbench is explicitly deferred rather than retired, and no command-inventory change authorizes deleting its schema contract. | retained-deferred | `go test ./internal/amazon/schema ./internal/amazon/listing -count=1`; exact intersection report |
| `internal/amazonlisting` (deferred workbench service and DTOs) | `internal/app/httpapi` Amazon Listing feature builder | `internal/app/httpapi/feature_builder_amazonlisting.go`, route tests, schema migration code, and `cmd/product-listing-api` README reference the package; README/current-refactoring-status §2.2 says the full Amazon workbench remains deferred, so active API compatibility must remain. | retained-deferred | `go test ./internal/amazonlisting/... ./internal/app/httpapi -count=1`; `go build ./cmd/product-listing-api` |
| `internal/temu` (2 declarations) | `cmd/temu-listing` and `internal/platforms/temu` | README §2.2 and `docs/refactoring/current-refactoring-status.md` §2.2 retain TEMU runtime correctness while deferring a full workbench; `cmd/temu-listing/main.go`, `internal/platforms/temu/module.go`, and `config/*.yaml` provide concrete runtime/config owners. | retained-configuration-specific | `go test ./internal/temu ./internal/platforms/temu -count=1`; maintained command builds |
| `internal/temu/ai` (30 declarations) | TEMU AI/property mapping stage | The package is imported by the TEMU processor and has an enabled-platform configuration boundary; the deferred product statement does not retire the existing runtime stage. | retained-configuration-specific | `go test ./internal/temu/ai ./internal/temu -count=1`; four-way intersection evidence above |
| `internal/temu/api` (17 declarations) | TEMU API client packages | `internal/temu` runtime construction imports the client APIs; `config/config-dev.yaml`, `config/config-test.yaml`, and `config/config-prod.yaml` retain TEMU settings and queue names. | retained-configuration-specific | `go test ./internal/temu/api/... -count=1`; `go build ./cmd/temu-listing` |
| `internal/temu/context` (2 declarations) | TEMU task context boundary | `internal/temu/task_handler.go` constructs and attaches `TemuTaskContext`; this is runtime-owned context code, not a detached tree. | retained-configuration-specific | `go test ./internal/temu/context ./internal/temu -count=1` |
| `internal/temu/image` (15 declarations) | TEMU image processing stage | Imported from TEMU product processing and retained by the official TEMU runtime/deployment owner; full workbench deferral does not remove image processing used by the runtime. | retained-configuration-specific | `go test ./internal/temu/image ./internal/temu -count=1`; `go build ./cmd/temu-listing` |
| `internal/temu/product` (20 declarations) | TEMU product commit/validation stage | Package is part of the TEMU processor graph and is covered by the runtime's platform selection/configuration; no retirement authority exists for the package. | retained-configuration-specific | `go test ./internal/temu/product ./internal/temu -count=1` |
| `internal/temu/property` (82 declarations) | TEMU property validation pipeline | Property validators are invoked by TEMU product/AI stages; package-level code is retained for configured TEMU processing even though the broader workbench is deferred. | retained-configuration-specific | `go test ./internal/temu/property ./internal/temu -count=1` |
| `internal/temu/rules` (1 declaration) | TEMU product-rule owner | Package is an exact TEMU rule owner and remains reachable through the configured runtime contract; no command or deployment retirement evidence exists. | retained-configuration-specific | `go test ./internal/temu/rules ./internal/temu -count=1` |
| `internal/temu/scheduler` (12 declarations) | TEMU scheduler module | `internal/app/bootstrap/schedulers/dependencies.go` and `internal/temu/scheduler/*_test.go` are concrete scheduler owners; `config/*.yaml` retains scheduler flags. | retained-configuration-specific | `go test ./internal/temu/scheduler ./internal/app/bootstrap/schedulers -count=1` |
| `internal/temu/sku` (21 declarations) | TEMU SKU conversion/build stage | SKU builders are package-owned processing code under the official TEMU runtime; no complete-subgraph retirement reference exists. | retained-configuration-specific | `go test ./internal/temu/sku ./internal/temu -count=1` |
| `internal/temu/store` (1 declaration) | TEMU store adapter | Store handlers belong to the TEMU runtime's configured processor boundary and are not independently retired. | retained-configuration-specific | `go test ./internal/temu/store ./internal/temu -count=1` |
| `internal/temu/sync` (26 declarations) | TEMU sync/scheduler integration | `internal/app/runner`, `internal/app/bootstrap/resources`, and package-owned sync tests reference this owner; scheduler and sync settings remain in `config/*.yaml`. | retained-configuration-specific | `go test ./internal/temu/sync ./internal/app/runner ./internal/app/bootstrap/resources -count=1` |
| `internal/crawler/amazon` (88 declarations across exact root/browser/extractor/variations packages) | Amazon crawler runtime and `amazon-crawler-api` deployment owner | `docs/architecture/amazon-crawler-runtime-flow.md`, `docs/refactoring/product-sourcing-inventory.md`, Docker/Kubernetes `amazon-crawler-api` assets, and `scripts/push-amazon-crawler-api-dockerhub.ps1` provide concrete operational ownership; `internal/integration/crawler/amazon` adapts the legacy crawler. The missing historical command entrypoint is a deployment consistency follow-up, not proof that the crawler implementation is dead. | retained-configuration-specific | `go test ./internal/crawler/amazon/... ./internal/integration/crawler/amazon -count=1`; `go build ./cmd/product-listing-api ./cmd/listing-control-plane ./cmd/shein-listing ./cmd/temu-listing`; deployment/reference searches |
| `internal/integration/crawler/amazon` (5 declarations) | Product Sourcing Amazon source adapter | `internal/integration/crawler/amazon/processor.go` owns the adapter and its package test covers invocation/shutdown; product-sourcing inventory explicitly assigns source access and raw extraction to this boundary. The HTTP boundary is maintained by `internal/infra/httpx`. | retained-deferred | `go test ./internal/integration/crawler/amazon ./internal/product/sourcing ./internal/infra/httpx -count=1` |
| `internal/compatibility/listingkit` (compatibility facade; no Task 8 intersection rows) | ListingKit compatibility boundary | `internal/compatibility/listingkit/README.md` defines backward-compatible DTO/facade ownership and `preview_adapter_test.go` protects the contract. It is not a candidate for broad deletion merely because the active business owner moved inward. | retained-deferred | `go test ./internal/compatibility/listingkit -count=1`; compatibility README and test references |
| Ten exact operational command directories: `cmd/fingerprint-browser-installer`, `cmd/listing-scheduler`, `cmd/listingkit-identity-preflight`, `cmd/listingkit-owner-scope-dry-run`, `cmd/listingkit-owner-scope-exceptions`, `cmd/listingkit-schema-migrate`, `cmd/playwright-installer`, `cmd/product-listing-api-schema-migrate`, `cmd/shein-import-platform-recovery`, `cmd/shein-login-worker` | `.github`, `deployments`, or `scripts` owner per command | `tests/repository_structure_test.go` and `docs/development/repository-structure.md` enforce the maintained inventory and owner evidence; each command is separately referenced by its operational wrapper/deployment. These are not product-runtime dead code. | retained-configuration-specific | `go test ./tests -run 'TestCmdContainsOnlyOfficialEntrypoints|TestOperationalCommandsHaveDeploymentBuildOrScriptOwner' -count=1`; maintained command inventory |

Allowed decisions: `removed`, `reconnected-defect`, `retained-deferred`, `retained-generated-contract`, `retained-configuration-specific`, and `detector-limitation`.

## Task 10 clone classification policy

The post-cleanup normalized jscpd run `.local/code-health/20260813-021313565/jscpd.txt`
reported 425 above-threshold pairs. The report is intentionally read as a candidate
set, not a deletion list. Pairs involving `*_test.go`, frontend test files, generated
contracts, compatibility facades, or operational wrappers are retained under their
existing owner because their entrypoint, fixture, or deployment semantics differ.
Cross-owner pairs are retained when the shared shape crosses marketplace policy,
tenant/error taxonomy, retry/transaction behavior, or runtime ownership boundaries;
the owner-specific rows above document those boundaries. Same-owner, behavior-neutral
deduplication is consolidated only when a characterization test protects ordering,
error wrapping, and persistence effects. No unclassified handwritten same-owner pair
remains after the Task 6/7 consolidations and the config helper extraction above.
