## Task Processor Framework Phase 82 Plan

### Objective

Run a new architecture-level hotspot discovery pass after the closure of the shared readiness contract line.

### Discovery Focus

Favor candidates that still combine:

- multi-consumer shared policy
- duplicated cross-flow contract wiring
- builder/runtime overlap with unclear ownership
- repeated outward adaptation logic with no explicit shared seam

### Candidate Areas

Strong candidates should be sought in:

1. submit and preview architecture around SHEIN / ListingKit state bridges
2. workflow/adapter boundaries where direct and temporal flows still mirror each other
3. any other ListingKit production area where a shared contract is still only implicit

### Verification

Any next candidate should be backed by:

- clear structural evidence from source
- at least one multi-consumer or cross-flow path
- a boundary test that can lock the seam once extracted
