# Generated TypeScript output: node web/listingkit-ui/node_modules/@hey-api/openapi-ts/bin/run.js -i docs/api/listingkit-asset.openapi.yaml -o web/listingkit-ui/src/lib/api/generated -p @hey-api/typescript
# Deterministic regeneration and drift verification: make api-contract-check
openapi: 3.1.0
info:
  title: ListingKit Read API
  version: 2.0.0
  description: |
    ListingKit reads an immutable Product Snapshot and the product's approved
    asset inventory. Image generation and approval are owned by ImageAgent.
paths:
  /api/v1/listing-kits/tasks/{task_id}/preview:
    get:
      operationId: getListingKitPreview
      summary: Read a ListingKit preview assembled from product facts and approved assets.
      parameters:
        - $ref: '#/components/parameters/TaskID'
        - name: platform
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TargetPlatform'
      responses:
        '200':
          description: ListingKit preview response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListingKitPreview'
        '400':
          description: Unsupported preview platform.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Task or requested platform preview was not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: Product Snapshot or approved assets are not ready.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
components:
  parameters:
    TaskID:
      name: task_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
  schemas:
    TargetPlatform:
      type: string
      enum: [amazon, shein, temu, walmart]
    ListingKitPreview:
      type: object
      required: [task_id, status, needs_review, created_at]
      properties:
        task_id:
          type: string
        status:
          type: string
        selected_platform:
          $ref: '#/components/schemas/TargetPlatform'
        platforms:
          type: array
          items:
            $ref: '#/components/schemas/TargetPlatform'
        needs_review:
          type: boolean
        catalog:
          $ref: '#/components/schemas/ProductSnapshot'
        approved_asset_inventory:
          $ref: '#/components/schemas/ApprovedAssetInventory'
        created_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
    ProductSnapshot:
      type: object
      properties:
        title:
          type: string
        brand:
          type: string
        category_path:
          type: array
          items:
            type: string
        description:
          type: string
        selling_points:
          type: array
          items:
            type: string
        seo_keywords:
          type: array
          items:
            type: string
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/ProductAttribute'
        variants:
          type: array
          items:
            $ref: '#/components/schemas/ProductVariant'
        images:
          type: array
          items:
            $ref: '#/components/schemas/ProductSnapshotImage'
    ProductAttribute:
      type: object
      properties:
        name:
          type: string
        value:
          type: string
    ProductVariant:
      type: object
      properties:
        source_id:
          type: string
        title:
          type: string
        sku:
          type: string
        stock:
          type: integer
        barcode:
          type: string
        is_default:
          type: boolean
        images:
          type: array
          items:
            $ref: '#/components/schemas/ProductSnapshotImage'
    ProductSnapshotImage:
      type: object
      properties:
        url:
          type: string
          format: uri
        role:
          type: string
    ApprovedAssetInventory:
      type: object
      required: [scope, assets]
      properties:
        scope:
          $ref: '#/components/schemas/ApprovedAssetScope'
        assets:
          type: array
          items:
            $ref: '#/components/schemas/ApprovedAsset'
    ApprovedAssetScope:
      type: object
      required: [tenant_id, product_key]
      properties:
        tenant_id:
          type: string
        product_key:
          type: string
    ApprovedAsset:
      type: object
      required: [id, role, url]
      properties:
        id:
          type: string
        run_id:
          type: string
        plan_revision:
          type: integer
          format: int64
        slot_id:
          type: string
        attempt:
          type: integer
        role:
          type: string
          enum: [design, main, white_background, gallery]
        url:
          type: string
          format: uri
        source_asset_id:
          type: string
        width:
          type: integer
        height:
          type: integer
        operations:
          type: array
          items:
            type: string
    ErrorEnvelope:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string
