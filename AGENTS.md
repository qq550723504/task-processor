# Repository Instructions

## 基本原则

- 如果我的指令有问题，你要反馈，不能直接去执行。
- 解决问题时要从根上解决，不能只解决表面问题。
- 在开发过程中发现项目已有的项目架构设计有问题，要及时指出来，不能忽略。
- 有开源实现，绝不重复造轮子；同时优先复用成熟开源实现或仓库现有能力，避免创建重复的本地基础设施。

## 开发准入与停止条件

开始编码前必须先判断任务是有界修改还是架构敏感修改。满足任一条件即属于架构敏感修改：

- 跨越 3 个及以上独立子系统；
- 跨越多个一致性边界，例如数据库、外部服务、消息队列、缓存、文件系统或浏览器状态中的两个及以上；
- 新增或修改状态机、补偿、对账恢复、授权边界、租户边界或破坏性操作；
- 预计超过 30 个范围相关文件；
- 预计新增超过 1,500 行生产代码，或生产代码变更超过 2,500 行；
- 同一个变更同时包含基础设施重构和面向用户的功能交付。

命中停止条件时不得继续堆代码。必须先拆分任务，或记录不能安全拆分的原因并进入架构设计与独立评审。开发过程中才发现超限或隐藏复杂度时，同样必须停止并重新分类。

文件规模阈值是风险触发器，不是质量目标。不得通过压缩代码、合并职责或人为拆分提交规避门禁。

## 架构敏感设计要求

架构敏感设计在实施前必须明确：

- 业务、安全和租户不变量；
- 权威数据所有者以及事务边界；
- 状态、事件、前置条件、转换和持久化效果；
- 幂等身份，以及相同键不同载荷的处理；
- 每个持久化边界前后失败、响应丢失、重试、重启、取消和并发的结果；
- 自动恢复的触发条件、可达入口和唯一责任方；
- 授权撤销、跨企业上下文和缓存漂移行为；
- 请求大小、超时、deadline 和资源耗尽边界；
- 每条不变量对应的验证证据。

涉及多个持久化步骤时必须提供故障矩阵，至少包含：失败边界、失败后的持久状态、重试身份与结果、恢复责任方、验证用例。只写“支持幂等”“失败回滚”或“最终恢复”不满足准入要求。

所有效果位于同一数据库时，必须先评估共享事务或 Unit of Work。确实跨越持久化边界时，必须先评估仓库已有能力或成熟开源方案，例如 transactional outbox、Saga 或项目已有的 Temporal，再决定是否实现新的恢复协议。

## 独立评审要求

- 架构敏感设计必须经过独立设计评审；设计编写任务不能批准自己的设计。
- 独立评审必须由新的人工评审者或全新上下文的 Codex 任务执行，目标是尝试推翻设计，而不是润色设计。
- 评审必须覆盖部分持久化、响应丢失、同键与异键并发、陈旧快照、多标签页或 Cookie 漂移、授权撤销、慢请求和 sibling paths。
- 模型名称、推理强度或测试数量本身不能作为独立评审证据。

## 实施与 PR 约束

- 使用 TDD：先写能捕获目标缺陷的测试并确认红灯，再写最小实现使其变绿。
- 将架构敏感工作拆成可独立验证、可回滚的小切片；一致性基础设施必须先于 API 和 UI 消费者落地。
- 开发阶段保持 PR 为 Draft；稳定并完成本地验证后再进入完整评审。
- 收到评审问题后先检查所有同根因的 sibling paths，再批量修复，不能逐条打补丁制造新的评审循环。
- 修复改变状态机、事务边界、恢复协议或公共契约时，必须退回设计阶段重新评审。
- 超出 CI 规模阈值时只能使用 `architecture-approved` 标签加上维护者或管理员对当前 head SHA 的 `APPROVED` review 豁免规模门禁。PR 必须记录批准维护者、设计链接以及不能安全拆分的原因。
- `architecture-approved` 不能豁免设计、独立评审、故障矩阵、测试或安全要求。

## 指令层级

子目录 AGENTS.md 可以增加适用于局部目录的规则，但子目录 AGENTS.md 不得弱化本文件中的根因分析、架构问题报告、开源复用、开发停止条件、独立设计评审和验证要求。
