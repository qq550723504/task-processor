# SHEIN开发环境配置
processor:
  maxRetries: 3
  timeout: 300

worker:
  concurrency: 10
  bufferSize: 100
  taskInterval: 60  # 60秒获取一次任务

server:
  port: 8080  # SHEIN使用8080端口

openai:
  apiKey: "sk-qns4hBrljHkJ520vwwA2508c7Dj3Oe0zGlh7oq7FWkcWXkx4"
  model: "gemini-2.0-flash"
  baseURL: "https://yunwu.ai/v1"
  timeout: 120

management:
  baseURL: "http://getway.linkcloudai.com"
  clientID: "go-listing"
  clientSecret: "go-listing-secret"
  tokenURL: "http://getway.linkcloudai.com/admin-api/system/oauth2/token"
  scopes: ["user.read"]
  #userID: 1001
  #storeIDs: [2001, 2002]  # SHEIN店铺ID列表

autoPricing:
  enabled: true
  interval: 300  # 5分钟
  batchSize: 100

amazon:
  enabled: true
  headless: true
  browserPath: "./chrome/chrome.exe"
  poolSize: 3
  zipcodes:
    US: "94107"
    IT: "00118"
  viewportWidth: 1920
  viewportHeight: 1080
  proxyServer: ""

platform:
  type: "web"  # web界面模式