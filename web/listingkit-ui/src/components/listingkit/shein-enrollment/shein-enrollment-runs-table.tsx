"use client";

export function SheinEnrollmentRunsTable() {
  return (
    <div className="rounded-2xl border border-dashed border-zinc-300 bg-white px-4 py-8 text-sm text-zinc-500">
      当前后端还没有报名记录列表接口，这个标签先保留入口，等 run list endpoint 接上后再展示历史记录。
    </div>
  );
}
