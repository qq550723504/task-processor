name: Development Admission Base Signal

on:
  push:

permissions: {}

jobs:
  signal:
    name: Signal base branch update
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Record successful base signal
        run: echo "Base branch update signal received for ${GITHUB_REF_NAME}"
