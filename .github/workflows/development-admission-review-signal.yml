name: Development Admission Review Signal

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

permissions: {}

jobs:
  signal:
    name: Signal review state change
    runs-on: ubuntu-latest
    steps:
      - name: Complete without privileged repository access
        run: echo "Development Admission review signal received"
