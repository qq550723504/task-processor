# Set a fresh, unused project name before running Compose.
# This is deliberately blank: a copied .env must fail closed until an operator
# generates and verifies a new value as described in README.md.
# COMPOSE_PROJECT_NAME=
