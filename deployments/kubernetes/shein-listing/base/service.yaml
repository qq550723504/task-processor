apiVersion: v1
kind: Service
metadata:
  name: shein-listing
  namespace: task-processor
  labels:
    app: shein-listing
spec:
  selector:
    app: shein-listing
  ports:
    - name: http
      port: 8081
      targetPort: http
    - name: metrics
      port: 8082
      targetPort: metrics
  type: ClusterIP
