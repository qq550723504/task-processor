apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: shein-listing-shard
  namespace: task-processor
  labels:
    app: shein-listing-shard
    shein-listing-role: service
spec:
  serviceName: shein-listing-shard-headless
  replicas: 8
  podManagementPolicy: Parallel
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      partition: 0
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: shein-listing-shard
  template:
    metadata:
      labels:
        app: shein-listing-shard
        shein-listing-role: service
        shard-mode: auto
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/agent
                    operator: In
                    values: ["true"]
                  - key: task-processor/crawler-tier
                    operator: In
                    values: ["heavy"]
      containers:
        - name: shein-listing
          image: xuwei190/task-processor-shein-listing:9078f71b-brandblank-20260623-1336
          imagePullPolicy: IfNotPresent
          command: ["/app/listing-service"]
          args:
            - -config
            - /app/config/config-shein-listing.yaml
            - -log-level
            - info
          ports:
            - name: http
              containerPort: 8081
            - name: metrics
              containerPort: 8082
          env:
            - name: TZ
              value: Asia/Shanghai
            - name: TASK_PROCESSOR_ENV
              value: prod
            - name: TASK_PROCESSOR_RABBITMQ_NODE_NODE_ID
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            - name: TASK_PROCESSOR_RABBITMQ_NODE_ROLE
              value: task
            - name: TASK_PROCESSOR_RABBITMQ_NODE_USE_STORE_QUEUES
              value: "true"
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_ENABLED
              value: "true"
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_ROLE
              value: worker
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_PLATFORM
              value: shein
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_INTERVAL
              value: "30"
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_PAGE_SIZE
              value: "200"
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_LOCK_KEY
              value: listing:queue:auto-shard:lock
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_LOCK_TTL
              value: "25"
            - name: TASK_PROCESSOR_RABBITMQ_AUTO_SHARD_CANDIDATE_NODES
              value: shein-listing-shard-0,shein-listing-shard-1,shein-listing-shard-2,shein-listing-shard-3,shein-listing-shard-4,shein-listing-shard-5,shein-listing-shard-6,shein-listing-shard-7
            - name: TASK_PROCESSOR_RABBITMQ_PROCESSING_TIMEOUT_WATCHDOG_ENABLED
              value: "false"
            - name: TASK_PROCESSOR_RABBITMQ_STALE_QUEUED_WATCHDOG_ENABLED
              value: "false"
            - name: TASK_PROCESSOR_REDIS_DB
              value: "9"
            - name: TASK_PROCESSOR_RABBITMQ_NODE_HEALTH_CHECK_PORT
              value: "8081"
            - name: TASK_PROCESSOR_RABBITMQ_NODE_METRICS_PORT
              value: "8082"
          envFrom:
            - secretRef:
                name: shein-listing-secret
          resources:
            requests:
              cpu: "1"
              memory: "2500Mi"
            limits:
              cpu: "3"
              memory: "5Gi"
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 300
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 6
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 90
          volumeMounts:
            - name: app-config
              mountPath: /app/config/config-shein-listing.yaml
              subPath: config-shein-listing.yaml
            - name: app-tmp
              mountPath: /app/tmp
            - name: app-logs
              mountPath: /app/logs
      volumes:
        - name: app-config
          configMap:
            name: shein-listing-config-heavy
        - name: app-tmp
          emptyDir: {}
        - name: app-logs
          emptyDir: {}
