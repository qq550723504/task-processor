apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: casdoor
spec:
  rules:
    - host: id.staging.shuomiai.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: casdoor
                port:
                  name: http
  tls:
    - hosts:
        - id.staging.shuomiai.com
      secretName: id-staging-shuomiai-tls
