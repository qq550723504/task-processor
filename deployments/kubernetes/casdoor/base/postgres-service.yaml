apiVersion: v1
kind: Service
metadata:
  name: casdoor-postgres
spec:
  # Private in-namespace database service; no public exposure.
  type: ClusterIP
  selector:
    app: casdoor-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
