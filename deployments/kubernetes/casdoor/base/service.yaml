apiVersion: v1
kind: Service
metadata:
  name: casdoor
  namespace: casdoor
  labels:
    app.kubernetes.io/name: casdoor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: casdoor
  ports:
    - name: http
      port: 8000
      targetPort: http
