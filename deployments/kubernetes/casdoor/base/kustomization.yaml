apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - deployment.yaml
  - service.yaml
  - rate-limit-middleware.yaml
  - ingress.yaml

images:
  # Digest pinning happens in overlays after staging verification.
  - name: casbin/casdoor
    newTag: v3.143.0
