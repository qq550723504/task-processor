apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: listingkit-workbench
spec:
  rules:
    - host: pod.shuomiai.com
      http:
        paths:
          - path: /api/v1/listing-kits/integrations/zitadel/sms
            pathType: Prefix
            backend:
              service:
                name: product-listing-api
                port:
                  name: http
          - path: /img
            pathType: Prefix
            backend:
              service:
                name: imgproxy
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: listingkit-ui
                port:
                  name: http
  tls:
    - hosts:
        - pod.shuomiai.com
      secretName: pod-shuomiai-com-tls
