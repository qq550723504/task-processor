apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-listing-api
  labels:
    app: product-listing-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-listing-api
  template:
    metadata:
      labels:
        app: product-listing-api
    spec:
      nodeSelector:
        task-processor/crawler-tier: heavy
      containers:
        - name: product-listing-api
          image: xuwei190/task-processor-product-listing-api:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: TASK_PROCESSOR_BROWSER_PATH
              value: /opt/fingerprint-chrome/chrome-144/chrome
            - name: TASK_PROCESSOR_LISTINGKIT_ZITADEL_MEMBER_INVITATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: listingkit-member-invitation-secret
                  key: TASK_PROCESSOR_LISTINGKIT_ZITADEL_MEMBER_INVITATION_TOKEN
            - name: TASK_PROCESSOR_SDS_LOGIN_SERVICE_DEFAULT_HEADLESS
              valueFrom:
                configMapKeyRef:
                  name: listingkit-workbench-config
                  key: TASK_PROCESSOR_SDS_LOGIN_SERVICE_DEFAULT_HEADLESS
            - name: TASK_PROCESSOR_SHEIN_LOGIN_SERVICE_DEFAULT_HEADLESS
              valueFrom:
                configMapKeyRef:
                  name: listingkit-workbench-config
                  key: TASK_PROCESSOR_SHEIN_LOGIN_SERVICE_DEFAULT_HEADLESS
          ports:
            - name: http
              containerPort: 8085
          envFrom:
            - configMapRef:
                name: listingkit-workbench-config
            - secretRef:
                name: listingkit-workbench-secret
                optional: true
          volumeMounts:
            - name: tmp
              mountPath: /app/tmp
            - name: logs
              mountPath: /app/logs
            - name: listingkit-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 900
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 90
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
        - name: listingkit-data
          emptyDir: {}
