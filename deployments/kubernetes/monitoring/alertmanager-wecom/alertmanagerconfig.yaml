apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: task-processor-wecom
  namespace: task-processor
  labels:
    app.kubernetes.io/name: task-processor-wecom
    app.kubernetes.io/part-of: task-processor
spec:
  route:
    groupBy:
      - alertname
      - service
      - severity
    groupWait: 30s
    groupInterval: 5m
    repeatInterval: 2h
    receiver: wecom-webhook
    matchers:
      - name: service
        value: amazon-crawler-api|shein-listing
        matchType: "=~"
  receivers:
    - name: wecom-webhook
      webhookConfigs:
        - url: http://alertmanager-wecom.monitoring.svc.cluster.local:8080/webhook
          sendResolved: true
          maxAlerts: 10
          timeout: 10s
